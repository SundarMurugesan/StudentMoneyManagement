package com.example.studentmoneymanagement;

import android.graphics.Canvas;

public interface CustomLineGraph {
	abstract void drawOverLay(Canvas canvas);
	abstract void drawBackgroundGraph(Canvas canvas);
}
